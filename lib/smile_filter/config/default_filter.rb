# frozen_string_literal: false

module SmileFilter
  module UserFilter
    module_function
  #  UserFilter.#exec が定義されると list.txt に先立って処理されます。
  # def exec(chats)
  #   「使用例」の◇はここに挿入
  #   chats.each do |chat|
  #     next if chat.empty?
  #       「使用例」の〇はここに挿入
  #   end
  # end
  end
end
